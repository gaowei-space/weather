language: php  # 运行的环境平台

php:  # 需要测试的PHP版本
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4
  - 8.0
  - 8.1

dist: trusty   # 系统版本
sudo: false  # 是否使用 sudo 模式

# 执行前需要运行的命令
install: travis_retry composer install --no-interaction --prefer-source

# 主要构建命令
script: vendor/bin/phpunit --verbose